extern fun write(value: Any): Void

foreach index in 0..15 {

    write("$index - ")
    if (index % 3 == 0) {
        write("foo")
    }
    if (index % 5 == 0) {
        write("bar")
    }
    write("\n")
}
